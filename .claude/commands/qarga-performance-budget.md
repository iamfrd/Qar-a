---
description: Measure Qarğa's built web assets against approved performance budgets; null/unmeasured targets remain NOT CONFIGURED rather than fabricated.
---

Apply `qarga-web-performance-budget`. Run the production build if appropriate and approved, then `npm run web:budget`. Report measured asset sizes and configured thresholds. Do not invent Core Web Vitals or bundle targets. Propose baseline collection before creating a blocking gate.
