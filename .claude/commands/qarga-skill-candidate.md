---
description: Build one approved skill candidate or edit one existing skill, without promoting it, then hand it to independent evaluation.
---

Require an approved skill-evolution research/proposal record and a completion contract. Use `qarga-system-improver` with `qarga-skill-authoring` and `qarga-anti-overfitting`. Modify only the approved skill/resources and required registry candidate metadata. Run validation, record the candidate, and stop before promotion. The candidate author must not perform the independent benchmark.
