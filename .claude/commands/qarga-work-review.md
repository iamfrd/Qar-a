---
description: Review Work OS integrity, stalled dependencies, open owner decisions, blocked tasks, scoring linkage, and whether completed cards have real evidence.
---

Use `qarga-work-os`, `qarga-independent-review`, `qarga-performance-governance`, and `qarga-project-memory-governance`.

Run `npm run work-os:validate`, `npm run work-os:summary`, `npm run task:review`, and inspect open project-memory items. Report cards that are blocked, stale, missing evidence, missing a reviewer, or waiting for an owner decision. Never convert missing evidence into a pass.
