# Playwright Integration

The E2E structure and smoke test are included. The dependency/browser binaries are not silently installed because that changes the project lockfile and runtime footprint. Follow `tests/e2e/README.md` after project-owner approval.
