# Qarğa Product Analytics Contract

The planning contract is `.claude/analytics/event-contract.json`. It defines candidate events because they support decisions, not because data is easy to collect. Event status `planned` or `future` is not proof that instrumentation exists.

Before implementation, review privacy/legal requirements, server/client source of truth, deduplication, IDs, event validation, and data retention. Unknown baselines remain unknown until measured.
