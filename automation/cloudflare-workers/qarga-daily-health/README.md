# Qarğa Daily Health Worker

Optional read-only 7/24 health template. It is not deployed by this package. Configure approved health endpoints and an optional reporting webhook through Cloudflare secrets/variables, review schedule/cost/permissions, then deploy manually after project-owner approval. It performs no code changes, merges, deploys, refunds, or production-data mutation.
